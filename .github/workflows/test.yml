name: "test-pr-supporter"
on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - run: npm ci
    - name: test
      uses: Im-D/pr-supporter@action
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
